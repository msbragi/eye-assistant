# Build output
gemmalink
gemmalink.exe

# Model files (too large for git)
models/*.gguf
models/*.bin

# Uploaded images (runtime temp files)
uploads/

# SSL certificates (generated locally)
.ssl/cert.pem
.ssl/key.pem

# OS / editor
.DS_Store
Thumbs.db
.vscode/

# llama-server binaries (downloaded separately)
bin/linux/llama-server
bin/windows/llama-server.exe
